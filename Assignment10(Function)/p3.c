#include<stdio.h>

int main()
{
    int a,num,digit;
    printf("\nEnter a Number:");
    scanf("%d",&num);
    printf("\nEnter a digit");
    scanf("%d",digit);

    int r;
    while(num>0)
    {
        r=n%10;
        if(r==d)
           printf("\n%d",r);
        n=n%10;
    }
//    printf("\n%d",r);
    return r;
}

}
